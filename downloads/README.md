# Download directory

Generated downloads and ASP CPW payloads are intentionally excluded from Git.

Copy `manifest.example.json` to `manifest.json`, then update its filename, size,
SHA-256 checksum, description, and availability. Place only files you have the
right to distribute in this directory.
